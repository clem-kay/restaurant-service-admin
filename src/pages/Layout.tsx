import Footer from "@/components/Footer";
import Navbar from "@/components/Navbar";

const Layout = () => {
    return (
        <div>
            <Navbar/>
            <Footer/>
        </div>
    );
};

export default Layout;